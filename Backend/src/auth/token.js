const secrect  = "PAUL-secret-key";

const userTypes = {
    ADMIN : 'Admin',
    CUSTOMER : 'Customer'
}

const AccountTypes = {
    SAVINGS : 'Savings Account',
    ZERO : 'Zero Account'
};

module.exports = {
    secrect : secrect,
    userTypes : userTypes,
    AccountTypes : AccountTypes
}